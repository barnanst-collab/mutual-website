PostSwap build stamp
====================
BUILD: 2026-07-11c
DATE:  2026-07-11

Email notifications
-------------------
1. Run supabase-schema.sql (creates profiles + email_queue).
2. Optional: deploy Edge Function supabase/functions/dispatch-emails
   and set RESEND_API_KEY for real delivery.
3. Profile → save email + toggles (DM / state swaps / interest).
4. New DMs and matching swaps enqueue rows in email_queue.

Redeploy checklist
------------------
1. Upload / publish these files from C:\Users\Paul\mutual-website:
   - index.html
   - supabase.js
   - app.js
   - styles.css
   - supabase-schema.sql (optional docs)
   - assets/* (logo, favicon, video)

2. After deploy, hard refresh (Ctrl+F5).

3. In DevTools console confirm:
   PostSwapDB ready
   PostSwapDB build: 2026-07-11a

4. Probe inserts:
   await PostSwapDB.testAnonInserts()

Cache bust query on assets in index.html: ?v=20260711a
